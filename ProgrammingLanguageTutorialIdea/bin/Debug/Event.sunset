EXPECTED_TYPES(EvtArgs)
import(List<FUNCPTR>)

List<FUNCPTR> pointers becomes new(List<FUNCPTR>)

func(FUNCPTR ptr) addFunction { pointers.add(ptr) }

func(EvtArgs args) raise { 
	
	foreach(ptr,pointers.toArray) {
		
		callptr(ptr,args)
		
	}
	
}
